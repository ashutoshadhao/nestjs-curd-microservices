{
  "name": "@app/shared",
  "version": "0.0.1",
  "description": "Shared resources for NestJS microservices",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "@nestjs/swagger": "^11.1.1",
    "class-validator": "^0.14.0",
    "reflect-metadata": "^0.2.2"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0 || ^11.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
